r"""
Returns the current version of the `{{ module_name }}` module through the `__version__` variable.
"""

from __future__ import annotations

from importlib.metadata import version


__version__ = version("{{ module_name }}")
